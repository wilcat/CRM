// server.js
// Ponto de entrada do Fluxo ERP. Toda a logica esta organizada em src/ —
// este arquivo apenas delega para src/server.js (mantido na raiz por
// compatibilidade com os guias de deploy, que usam `node server.js`).

require('./src/server');
